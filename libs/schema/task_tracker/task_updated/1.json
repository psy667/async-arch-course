{
  "$schema": "http://json-schema.org/draft-07/schema#",

  "type": "object",
  "title": "task_tracker.task_updated.v1",
  "description": "Task updated CUD event",

  "definitions": {
    "event_data": {
      "type": "object",
      "$id": "#event_data",
      "properties": {
        "task_id": { "type": "string" },
        "task_description": { "type": "string" },
        "task_assigned_to": { "type": "string" },
        "task_status": { "type": "string" },
        "task_fee": { "type": "string" },
        "task_reward": { "type": "string" }
      },
      "required": [
        "task_id",
        "task_description",
        "task_assigned_to",
        "task_status",
        "task_fee",
        "task_reward"
      ]
    }
  },
  "properties": {
    "event_id": { "type": "string" },
    "event_version": { "type": "integer" },
    "event_name": { "type": "string" },
    "event_timestamp": { "type": "integer" },
    "producer": { "type": "string" },
    "data": {"$ref": "#/definitions/event_data"}
  },
  "required": [
    "event_id",
    "event_version",
    "event_name",
    "event_timestamp",
    "producer",
    "data"
  ]
}